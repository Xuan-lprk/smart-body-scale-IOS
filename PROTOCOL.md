# AFU-WL 协议笔记

这里记录的是对特定设备实测广播和通知包的逆向结果，不是厂商官方协议。

## 已验证设备

- 广播名：`AFU-WL-TZ-A1`
- 设备类别：体脂秤
- subtype：`7`
- GATT 服务：`FFB0`
- Notify 特征：`FFB2`

其他名称、型号、固件或 subtype 即使外观相同，也不保证兼容。

## 设备发现

设备可能同时广播：

- `FFB0` 服务；
- Apple iBeacon Manufacturer Data；
- UUID 为 `27AC` 的 Service Data。

对实测设备，协议识别数据需要由 Service Data 重建：

```text
UUID 27AC + payload 66 55 44 33 22 11 01
→ AC 27 66 55 44 33 22 11 01
```

其中 `0x27` 可拆分为连接类型、category `2` 和 subtype `7`。以上脱敏示例中的 MAC 字节反转后为 `11:22:33:44:55:66`。

因此 iOS 客户端不使用 GATT 服务作为扫描前置过滤，而是接收广播后再按名称和协议字段筛选。这与配套 CLI 的发现策略一致。

## 测量数据

连接后，客户端发现 `FFB0` 服务中的可通知特征并订阅数据。当前识别：

- `D5`：实时/稳定体重；
- `D6`：一个或多个 ADC/阻抗值；
- `D8`：历史数据；
- `DF`：设置相关数据。

`D6` 中的两个 ADC 都会保留。由于它们的物理含义尚未确认，App 允许用户选择 ADC 1 或 ADC 2 用于公式估算，不对两者求平均，也不宣称其中某个一定更准确。

## 安全边界

解析器只接受：

- 名称以 `AFU-WL` 开头；
- category 为 `2`；
- subtype 为 `7`；
- 测量通知具有已知的 `0xAC` 包装或已知传统包头。

协议仍可能随固件升级而变化。提交兼容性问题时，请删除个人资料后提供设备型号、固件版本和脱敏后的原始十六进制日志。
