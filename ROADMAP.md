# 产品方向与新功能路线图

本文整理 AFU-WL 客户端后续可能的发展方向，以及开发前需要确认的产品选择。它不是承诺全部实现的排期，而是一份用于取舍、Review 和提交 Issue 的长期规划。长期不变的产品原则见 [VISION.md](VISION.md)；路线图中的功能与顺序不得违背该宗旨。

## 一句话定位

一个本地优先、来源可追溯的身体测量数据中转与管理工具：

- 对已适配的 AFU-WL 体脂秤提供直接蓝牙读取；
- 通过 Apple 健康汇总其他品牌 App 已经共享的数据；
- 通过本地截图识别、文件或手动录入补充未共享的数据；
- 统一整理不同来源，同时保留原始值、来源和计算属性；
- 帮助用户识别重复、冲突、缺失、来源切换和长期变化；
- 在用户确认和授权后，把系统支持的本人指标补充到 Apple 健康；
- 不要求账号，不把健康数据上传到项目自己的服务器。

它不准备替代 Apple 健康，也不准备逆向市面上所有体重秤。重点是打通厂商数据孤岛，并补足 Apple 健康目前不够直观的部分：来源解释、重复识别、跨来源趋势、数据质量提示和可迁移备份。

## 当前建议：不要做“大而全健康管理”

经过重新梳理，项目最合适的定位不是另做一个 Apple 健康，也不是堆出很多身体指标，而是：

> **AFU-WL 的隐私友好数据入口，以及跨厂商身体测量数据的补充、审计、整理和解释工具。**

Apple 健康继续作为本人通用指标的系统级交换中心。本 App 负责五件事：

1. **接进来**：直接读取 AFU-WL，并读取其他 App 已共享的 HealthKit 数据；
2. **补进来**：让用户通过本地截图识别、文件或手动方式补充没有共享的数据；
3. **看来源**：说明数据来自设备、HealthKit、截图、文件还是手动输入；
4. **理清楚**：识别疑似重复、来源切换、缺失和冲突；
5. **看趋势、带得走**：提供来源可解释的趋势、合规的 HealthKit 回写和个人导出。

这五件事已经足以构成完整产品。它们之外的功能只有在确实增强这个闭环时才进入开发。

### “管理数据”具体包含什么

| 用户问题 | 本 App 应提供的能力 | 优先级 |
|---|---|---|
| 我的 AFU-WL 能不能不用厂商云端？ | 蓝牙测量、本地记录、可选写入 Apple 健康 | 核心，继续维护 |
| 这些数据是谁写入的？ | 来源 App、品牌提示、指标覆盖和最后更新时间 | 核心，第一版已完成 |
| 同一次称重是不是出现了两份？ | 疑似重复分组、依据说明、展开原始样本 | 核心，第一版已完成 |
| 我实际发生了什么变化？ | 统一时间线和带来源标记的趋势 | 下一步 |
| 换秤或换 App 后为什么突然跳变？ | 来源切换标记和数据断层提示 | 下一步 |
| 多个来源同时存在时看哪一个？ | 来源筛选；以后再增加首选来源 | 下一步 |
| 数据是否完整、可信？ | 缺失、异常跳变和同步中断提示 | 第二阶段 |
| 厂商 App 没有同步到 Apple 健康怎么办？ | 本地截图识别或手动录入，确认后保存和可选回写 | 核心闭环 |
| 我能不能带走自己的数据？ | JSON 备份和 CSV 导出 | 第二阶段 |
| ADC 1/2 哪个更接近阿福？ | 独立的 AFU 实验室，不混入日常首页 | 可选实验功能 |

### 不应成为产品主线的内容

- **更多体脂公式和几十个衍生指标**：会增加“看起来很专业”的感觉，却不一定增加可信度；
- **直接连接许多品牌秤**：协议维护成本极高，其他品牌优先通过 Apple 健康接入；
- **通用健康目标、打卡和提醒**：Apple 健康及厂商 App 已能完成，项目没有明显优势；
- **AI 健康解读**：容易越过数据整理边界，产生医疗暗示和额外隐私风险；
- **三套主题和大量动效**：在核心时间线完成前，不解决数据管理问题；
- **个人体脂校准**：数据门槛高、容易过拟合，只能保留为明确标注的实验；
- **账号、社区和排行榜**：与离线优先、个人数据控制的定位冲突。

### 一个重要限制

HealthKit 中稳定可获得的是“写入样本的 App 或来源信息”。它不保证每条记录都能精确告诉我们背后的实体秤型号。因此界面应该写“来源 App / 数据提供方”，不能承诺一定识别出具体硬件。

## 为什么可能有人需要

用户通常不会同时每天使用很多台秤，但可能经历以下情况：

- 更换过体重秤或厂商 App，历史数据分散在多个来源中；
- 同一次称重被厂商 App、快捷指令或其他同步工具重复写入；
- Apple 健康能展示数值，却不容易看出趋势变化是否来自“换秤”或“换算法”；
- 某些厂商只向 Apple 健康写入体重，不写入体脂等指标；
- 希望继续使用便宜的秤，但不愿把全部数据交给厂商云端；
- 希望导出一份可读、可迁移的个人数据副本。

因此，项目更合理的方向不是“管理很多台秤”，而是“解释和整理来自多个健康数据来源的记录”。

## 已经确认的产品原则

### 1. Apple 健康优先，用户确认导入补充

其他品牌的数据优先通过 HealthKit 读取。厂商未共享的数据，可以通过设备端截图识别、文件或手动录入补充。除当前已验证的 AFU-WL 外，不主动承诺逆向或直连更多秤，也不能直接读取其他 App 的沙盒数据库。

### 2. 本地优先

来源分析、重复检测和趋势计算均在设备本地完成。项目不建立账号系统、广告系统或自己的健康数据服务器。

### 3. 不自动删除健康数据

疑似重复、异常值和来源冲突只提示。是否删除 Apple 健康中的记录，由用户在系统提供的界面中决定。

### 4. 区分测量、换算和估算

界面必须明确区分：

- 秤直接测得的体重和原始 ADC；
- 协议换算出的阻抗；
- 本地公式估算出的身体成分；
- 厂商 App 或其他设备写入 Apple 健康的参考结果。

### 5. 不作医疗承诺

趋势和数据质量提示只用于日常观察。项目不诊断疾病，也不把消费级 BIA 结果描述为医疗级测量。

## 功能方向

### 方向 1：健康数据来源总览

回答“这些体重和体脂是谁写进去的”。

计划能力：

- 按来源 App 和 bundle identifier 分组；
- 展示来源名称、识别品牌、指标类型、样本数和最后更新时间；
- 支持体重、BMI、体脂率和去脂体重；
- 对未知来源保留原始名称，不强行猜测品牌。

当前状态：第一版已经实现。

### 方向 2：疑似重复记录

回答“同一次称重是不是被写了两遍”。

计划能力：

- 检测不同 App 在相近时间写入的相近体重；
- 展示每条记录的来源、时间和数值；
- 允许调整查看范围；
- 后续可在 App 自己的趋势中折叠疑似副本，但不改动 HealthKit 原始数据。

当前状态：第一版使用“5 分钟内、相差不超过 0.1 kg”的保守规则，只提示不处理。

### 方向 3：统一身体数据时间线

把不同来源记录放在同一条可解释的时间线上。

计划能力：

- 按日期展示体重及可用的身体成分；
- 每条记录显示来源标签；
- 疑似重复记录默认归为一组；
- 能展开查看原始记录；
- 允许按来源和指标筛选。

价值：这是从“查看来源”走向“真正可日用”的关键页面。

### 方向 4：跨来源趋势

让用户看见变化，同时知道变化是否可能由数据来源改变造成。

计划能力：

- 7 天、30 天、90 天和全部时间范围；
- 体重、BMI、体脂率和去脂体重趋势；
- 移动平均与原始点切换；
- 在图表上标记来源切换；
- 某段数据突然更换来源时给出解释，而不是直接判断身体发生剧烈变化。

### 方向 5：来源筛选与偏好

解决“多个来源都存在时，趋势到底采用哪一条”。

计划能力：

- 查看全部来源；
- 只查看指定来源；
- 为某个指标设置首选来源；
- 首选来源缺失时，可选择显示其他来源或留空；
- 所有筛选只影响 App 展示，不修改 Apple 健康。

默认建议：先展示全部来源和清楚的来源标签，不在数据不足时自动宣布某个品牌更准确。

### 方向 6：数据质量与异常提示

指出“这条记录可能有问题”，但不制造健康焦虑。

可检查：

- 同一次称重的重复写入；
- 短时间内不合理的体重跳变；
- 体重存在但体脂等关联指标缺失；
- 单位或小数点异常；
- 来源 App 长时间停止同步；
- 同一来源算法结果出现明显断层。

提示必须说明判定依据，并允许用户忽略。第一版不做医学意义上的“健康异常”判断。

### 方向 7：AFU-WL 官方结果对照与 ADC 建议

这是当前秤的专属实验功能，不应用于其他品牌。

计划能力：

- 手动录入或从截图确认蚂蚁阿福的体重、体脂等参考结果；
- 保存同次测量的 ADC 1、ADC 2 和本地两套估算；
- 计算平均偏差、中位偏差、MAE 和 RMSE；
- 至少积累 10 个不同日期的合格样本后，才建议更接近厂商结果的 ADC；
- 明确说明“更接近阿福”不等于“更接近医学真实值”。

暂不自动校准公式，也不根据少量数据改写旧历史。

### 方向 8：导出、备份与迁移

让用户可以带走自己的数据。

计划能力：

- JSON 完整备份，用于 App 数据恢复；
- CSV 导出，用于表格分析；
- 通过系统文件选择器保存到本地或 iCloud Drive；
- 导入时按稳定标识去重；
- 不导出 HealthKit 权限、蓝牙配对标识或 Apple 开发团队信息。

### 方向 9：更完整的日用体验

在核心数据可信后再改善产品完成度。

可选能力：

- 测量阶段和连接状态更清楚；
- 趋势摘要与“相比上次”；
- 深色模式、动态字体和 iPad 多栏布局；
- 蓝牙故障排查页；
- 脱敏演示数据；
- 更可靠的本地数据库和旧数据迁移；
- 单元测试、UI 测试和自动构建。

## 建议开发顺序

### 阶段 A：完成真正的最小产品

目标：用户每天只打开这一个页面，就能看懂自己的身体测量记录。

1. 健康数据来源总览；
2. 疑似重复记录；
3. 统一身体数据时间线；
4. 基础来源筛选；
5. 体重和体脂趋势；
6. 在趋势中标记来源切换和疑似重复；
7. 完成手动录入；
8. 用一种脱敏厂商截图验证本地 OCR → 用户确认 → 保存的最小闭环。

完成标准：

- 能按时间查看本 App 和其他 HealthKit 来源；
- 每条记录都能解释来源；
- 疑似重复默认归组但可以展开；
- 趋势不会把换来源造成的断层伪装成身体变化；
- 厂商未共享的体重或体脂可以由用户确认后补充；
- 不修改或删除 Apple 健康原始数据。

### 阶段 B：增加数据控制能力

1. JSON 完整备份和 CSV 导出；
2. 缺失指标、异常跳变和停止同步提示；
3. 首选来源设置；
4. 导入去重和恢复；
5. 更可靠的本地持久化与迁移。

完成标准：用户能理解、筛选、备份和迁移自己的数据，不被锁在本 App 中。

### 阶段 C：可选的 AFU-WL 实验室

1. 官方结果手动对照；
2. 双 ADC 统计；
3. 样本质量判断；
4. 达到门槛后的 ADC 建议；
5. JSON / CSV 导出。

完成标准：ADC 选择基于用户自己的长期对照数据，而不是单次猜测。

### 阶段 D：只做必要的成品化

1. 完善测试和 CI；
2. 改善 iPad、深色模式和无障碍体验；
3. 补充演示数据、截图和贡献说明；
4. 根据真实使用反馈决定是否继续扩展。

不把“做完所有候选功能”作为完成标准。

## 下一步的具体页面

下一项最值得实现的不是更多设置，而是一个新的“数据”页面：

```text
数据
├── 摘要
│   ├── 最新体重 / 体脂
│   ├── 与上次相比
│   └── 当前主要来源
├── 趋势
│   ├── 7 天 / 30 天 / 90 天 / 全部
│   ├── 体重 / 体脂
│   └── 来源切换标记
└── 时间线
    ├── 按称重事件分组
    ├── 来源标签
    ├── 疑似重复折叠
    └── 展开原始样本
```

第一版只做体重和体脂。BMI 可以由体重与身高计算；去脂体重可以随后加入。水分、肌肉、骨量等没有统一 HealthKit 类型，继续留在 AFU 本地详情中，不强行混进跨品牌时间线。

### 建议的信息架构

底部导航收敛为：

1. **测量**：AFU-WL 连接和实时称重；
2. **数据**：统一摘要、趋势和时间线；
3. **我的**：资料、家庭成员、Apple 健康、来源与隐私设置。

现有“历史”由只显示本 App 记录，升级为“数据”页面的一部分。这样用户不需要理解“本地历史”和“HealthKit 历史”为什么是两套列表。

### 默认合并规则

- 不删除、不改写 HealthKit；
- 同一指标、不同来源、5 分钟内且体重差不超过 0.1 kg：标为疑似同次称重；
- 时间线将其折叠为一个事件，但允许展开所有原始样本；
- 趋势第一版取该组体重中位数，避免随意宣布某个来源最准确；
- 体脂结果不平均：并列保留来源，用户选择来源后再用于趋势；
- 本 App 自己写入 HealthKit 后产生的本地记录和 HealthKit 副本，应通过 UUID 或元数据识别，不能仅靠时间猜测；
- 所有自动判断都展示依据，并允许用户关闭折叠。

### 做到哪里可以先停

当以下五件事成立时，第一版“数据管理”需求已经满足：

1. AFU-WL 可以稳定测量并写入 Apple 健康；
2. 能查看最近一年的体重和体脂统一时间线；
3. 每条记录能看见来源；
4. 疑似重复会归组且可以展开；
5. 趋势能区分真实变化和来源切换。

到这里应先真实使用一至两周，再决定是否扩展更多厂商截图模板、算法实验室、自动 ADC、主题或云同步。

## 开发前需要确认的问题

以下问题不必一次全部回答。进入对应阶段前再决定即可。

### 产品范围

1. App 的第一目标是服务你自己的 AFU-WL，还是让不使用 AFU-WL 的人也能作为 Apple 健康数据整理工具使用？
2. 是否接受“其他体重秤优先读取 HealthKit；缺失数据由用户截图、文件或手动补充；不逐个承诺蓝牙直连”作为长期边界？
3. 家庭成员是否继续只管理本 App 的 AFU-WL 记录，不混入 Apple 健康中的本人数据？

建议答案：先服务本人和 AFU-WL；跨品牌使用 HealthKit 优先、用户确认导入补充；家庭成员保持本地独立。

### 重复与来源规则

4. 疑似重复是否只提示，还是允许在 App 自己的趋势中默认折叠？
5. 用户手动指定首选来源后，其他来源是隐藏、淡化，还是仍完整展示？
6. 体重相同但体脂不同的记录，应视为同次称重的不同算法结果，还是两条独立记录？

建议答案：HealthKit 原始记录永不改动；趋势默认折叠但可以展开；体脂差异保留并标注来源。

### 趋势与提醒

7. 第一版趋势最重要的是体重，还是体重与体脂同时完成？
8. 是否需要“异常跳变”提醒？如果需要，应只提示数据质量，还是也解释可能的身体变化？
9. 是否需要日、周、月目标以及称重提醒？

建议答案：先做好体重趋势和来源解释；体脂随后加入；暂不做健康结论和打卡压力较强的提醒。

### 隐私与发布

10. 是否始终坚持无账号、无项目自建云端？
11. 是否允许匿名崩溃报告或使用统计？
12. 项目定位是个人实验、开源工具，还是未来可能上架 App Store 的产品？

建议答案：当前保持无账号、无网络统计的开源工具；真正准备上架时再单独评估隐私政策、品牌命名和法律风险。

## 暂时不做

- 逆向并直连所有热门体重秤；
- 调用阿里或其他厂商的私有云端接口；
- 自动删除或改写 Apple 健康记录；
- 用少量样本训练“更准确”的个人体脂公式；
- 宣称可以判断真实体脂或替代 DXA、医疗检查；
- 社区、排行榜、广告和账号体系；
- 在核心时间线完成前堆叠 AI 解读、复杂主题或大量装饰性功能。

## 已讨论但暂缓的功能

下面这些功能来自此前的三阶段成品化方案。它们没有被否定，只是不应排在“来源、重复记录、统一时间线和基础趋势”之前。

### 数据与测量可靠性

- 使用 SwiftData 保存成员、测量和官方参考；
- 从现有 `UserDefaults` 安全迁移，迁移成功前不删除旧数据；
- 每条记录保留双 ADC、双阻抗、资料快照、算法版本、质量状态和稳定原始包；
- 把测量生命周期明确为“等待 → 称重 → 体重稳定 → 等待阻抗 → 完成”；
- 只有离秤后才开始下一次测量，避免同一次上秤重复保存；
- 补充扫描超时、重连、信号强度、可读错误和故障排查。

### 官方结果录入与截图 OCR

- 使用 `PhotosPicker + Vision` 在设备本地识别蚂蚁阿福或其他已适配厂商截图；
- 不申请完整相册权限，也不上传截图；
- OCR 结果只能生成草稿，必须由用户核对后保存；
- 保存每个字段的识别置信度，对低置信度和小数点疑问作醒目标记；
- 使用脱敏截图测试字段缺失、顺序变化、重复文本和小数点误识别。

### 算法实验室

- ADC 1 和 ADC 2 的完整结果并排查看；
- 查看原始 ADC、换算阻抗、稳定包和公式版本；
- 展示误差散点图、时间趋势以及单次、成员和全局报告；
- 建立算法注册接口，让生产公式和实验公式并存；
- 实验公式不自动覆盖历史，也默认不写入 Apple 健康；
- DXA 等参考数据只用于保存和报告，不自动修改算法。

### 自动 ADC 与个人校准

- `手动`：始终由用户选择 ADC；
- `建议`：至少 10 个不同日期的合格样本后，提示 MAE 更低的一路；
- `自动`：只有用户主动启用，且胜出 ADC 改善达到预设门槛时才切换；
- 每次自动决定留下记录，不重新计算旧历史；
- 个人校准至少需要 30 个不同日期的合格样本；
- 必须按时间划分训练集和验证集，避免使用同一批数据自证效果；
- 校准只能描述为“贴近阿福的个人实验模型”，不能宣称更接近真实体脂；
- 校准结果独立展示，默认不写入 Apple 健康。

### 主题和高级界面

- `健康`：系统卡片、留白和柔和渐变；
- `科技`：深色背景、发光曲线和更明显的测量反馈；
- `数据`：弱装饰、高密度数字和专业图表；
- 三套主题共用设计令牌，不复制三套业务页面；
- 支持动态字体、深色模式、小屏和 iPad 横屏；
- 在核心数据页面稳定后，再评估 Liquid Glass 等新系统视觉能力。

### 演示、测试与 GitHub 展示

- 加入完全脱敏的演示模式和固定样本；
- 没有体脂秤的开发者也能查看测量结果、趋势和对照流程；
- 为协议、状态机、迁移、统计、HealthKit 和关键 UI 建立测试；
- 补充 README 截图、架构图、协议说明、贡献指南、Issue 模板和版本标签；
- CI 持续执行无签名构建和自动测试。

### 备份与未来同步

- 本地模式始终为默认；
- 通过系统文件选择器把 JSON / CSV 保存到本地或用户自己的 iCloud Drive；
- 预留 `BackupProvider` 一类的抽象，但当前不建立项目自己的云端账号；
- 只有具备稳定 Bundle ID、开发者计划资格和 iCloud Container 后，才评估 CloudKit 自动同步。

这些项目重新进入开发前，仍需经过本文最后的“新功能进入条件”，不能因为曾经讨论过就默认全部实现。

## 每项新功能的进入条件

开始实现前，应能回答：

1. 它解决了哪个真实问题？
2. Apple 健康是否已经足够解决？
3. 是否需要新的敏感数据权限？
4. 是否会修改或删除用户数据？
5. 没有对应厂商 App 或硬件时能否测试？
6. 是否有明确的完成标准和回归测试？
7. 如果功能无人使用，维护成本是否仍可接受？

如果这些问题没有清楚答案，功能先留在路线图，不进入代码。
