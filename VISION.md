# 项目宗旨

## 宗旨原文

> 本项目旨在成为一个本地优先、来源可追溯、结果可解释、数据可迁移的身体测量数据中转与管理工具。它直接读取已经验证兼容的 AFU-WL 体脂秤；从 Apple 健康读取其他 App 已经共享的身体测量；对未能进入 Apple 健康的数据，允许用户通过设备端截图识别、文件导入或手动录入进行补充，并在用户确认后保存。项目在不丢失原始来源、时间、单位和计算属性的前提下，将不同厂商的数据整理为统一记录，识别疑似重复、冲突、缺失和来源切换，通过可解释的时间线与趋势帮助用户长期管理数据；仅在用户明确授权后，把 Apple 健康支持、归属于本人且来源说明完整的指标写入 Apple 健康。项目默认不建立账号、不依赖自建云端、不上传健康数据，也不把消费级估算描述为医疗测量。

更简短地说：

> **让身体测量数据摆脱厂商孤岛，同时不丢失来源和可信边界。**

## 项目解决的问题

项目不是为了让一个人每天使用许多体重秤，而是解决身体测量数据在设备、厂商 App 和 Apple 健康之间不连续的问题：

- 有些体重秤必须依赖厂商账号或云端；
- 有些厂商 App 只向 Apple 健康写入体重，不写入体脂等结果；
- 有些厂商 App 完全不支持 Apple 健康；
- 更换秤或 App 后，历史记录被分散在不同位置；
- 同一次称重可能被多个 App 重复写入；
- 不同厂商对同一个指标使用不同算法，却在趋势中看起来像同一种数据；
- 用户难以导出、核对和迁移自己的完整历史。

项目的价值是“连接、整理和解释”，而不是制造更多看似精确的指标。

## 四条数据入口

| 入口 | 用途 | 基本规则 |
|---|---|---|
| 直接蓝牙读取 | 已验证兼容的 AFU-WL | 保存秤直接发送的数据、协议换算结果和算法版本 |
| Apple 健康读取 | 已经由其他 App 共享的数据 | 保留 HealthKit 样本 UUID、来源 App、时间和设备信息（如系统提供） |
| 截图或文件导入 | 厂商 App 未共享的数据 | 全程本地处理；识别结果只是草稿，必须由用户确认 |
| 手动录入 | 少量历史、OCR 失败或参考测量 | 明确标记为手动输入，不伪装成设备直接测量 |

除已经验证兼容的设备外，项目不承诺直接连接所有品牌体重秤。其他品牌优先使用 HealthKit；无法使用 HealthKit 时，再采用用户主动提供并确认的导入方式。

## 统一不等于抹平差异

统一记录至少需要保留：

- 测量时间和导入时间；
- 数值与原始单位；
- 指标的统一类型与厂商原始名称；
- 来源 App、bundle identifier 和可用的设备信息；
- 输入方式：蓝牙、HealthKit、截图、文件或手动；
- 数据属性：
  - 设备直接测量；
  - 协议换算；
  - 本地计算；
  - 厂商估算；
  - 用户录入；
  - 外部参考；
- 算法名称与版本（如果已知）；
- 原始样本或原始包的稳定标识；
- 是否经过用户确认；
- 是否疑似重复、冲突、缺失或异常；
- 是否已经写入 Apple 健康，以及对应的防重复标识。

项目可以把“体重”“体脂率”等共同概念放在同一时间线上，但不能因为名称相同，就宣称不同厂商的算法结果具有相同含义或准确度。

水分、肌肉、骨量、蛋白质、皮下脂肪和内脏脂肪等厂商私有或缺少通用 HealthKit 类型的指标，可以保存在 App 本地；必须保留原始名称和来源，不能硬塞进不相符的 Apple 健康类型。

## 整理和去重原则

- 永不因为“疑似重复”自动删除 Apple 健康或厂商原始记录；
- 合并只是一种 App 内展示方式，原始样本始终可以展开查看；
- 相近时间和相近体重只能说明“可能是同一次称重”，不能证明两条数据相同；
- 体重可以在明确规则下选择中位数用于展示，但必须保留原始值；
- 不自动平均来自不同算法的体脂率或其他身体成分；
- 来源偏好只影响 App 的展示和趋势，不改写原始数据；
- 本 App 的本地记录及其 HealthKit 副本应优先通过 UUID 和元数据对应，不能只靠时间猜测；
- 每个自动判断都应显示依据，并允许用户展开、忽略或关闭折叠。

## 写入 Apple 健康的原则

只有同时满足以下条件的数据才可以写入：

1. 用户已明确开启对应指标的 HealthKit 写入权限；
2. 数据归属于“本人”，不是家庭成员；
3. 指标存在正确对应的 HealthKit 类型；
4. 数值已经由用户确认或来自完整的稳定测量；
5. 写入样本携带来源、算法、ADC 或导入方式等必要元数据；
6. 已检查本 App 是否写过同一条记录，避免读出后再次回写形成循环。

体重应标记为直接测量或外部录入。BMI、体脂率和去脂体重若由本 App 计算，应明确携带算法信息。实验公式、个人校准结果和未经确认的 OCR 草稿默认不得写入 Apple 健康。

## 隐私承诺

- 默认不要求账号；
- 默认不建立项目自己的健康数据服务器；
- 蓝牙解析、来源识别、重复检测、趋势计算和截图 OCR 在设备本地完成；
- 使用系统选择器读取用户主动选择的截图或文件，不申请不必要的相册范围；
- 不加入广告 SDK、数据经纪服务或面向广告的统计；
- JSON 和 CSV 由用户主动导出，可保存到本地或用户自己的 iCloud Drive；
- 公开日志、Issue、截图和演示数据必须脱敏；
- 若未来引入可选崩溃报告或云同步，必须重新说明数据范围并单独征得同意，不能悄悄改变本宗旨。

## 可信度承诺

界面必须让用户看出数据属于哪一层：

1. **直接测量**：例如秤返回的体重和原始 ADC；
2. **协议换算**：例如从 ADC 得到的阻抗；
3. **公式估算**：例如本地 BIA 体脂率；
4. **粗略衍生**：例如由体脂按固定比例推算的部分指标；
5. **厂商参考**：来自厂商 App 的消费级算法结果；
6. **外部参考**：例如用户录入的 DXA 结果。

截图识别只负责减少录入工作，不证明截图内容真实或算法准确。厂商结果与本地结果更接近，也不等于更接近医学真实值。

项目不得：

- 宣称达到医疗级准确度；
- 把估算值写成秤直接测得；
- 把厂商算法结果当作权威真值；
- 用少量个人样本训练模型后宣称提高了真实准确性；
- 根据体重或体脂变化作出疾病诊断。

## Apple 健康与本地数据库的分工

Apple 健康是本人通用身体指标的系统级交换中心；App 本地数据库是更完整的来源与实验档案。

| 数据 | 推荐保存位置 |
|---|---|
| 本人的体重、BMI、体脂率、去脂体重 | Apple 健康和 App 本地索引 |
| AFU 双 ADC、阻抗、稳定原始包、算法快照 | 仅 App 本地 |
| 厂商私有身体指标 | App 本地 |
| OCR 草稿和未确认导入 | 仅 App 本地，且不写入趋势 |
| 家庭成员记录 | 仅 App 本地，不写入本人的 Apple 健康 |
| HealthKit 原始样本 | 仍由 Apple 健康保存，App 只读取和建立必要索引 |

App 不应复制出一个与 Apple 健康长期争夺“唯一真相”的第二数据库，也不能只依赖 Apple 健康而丢失双 ADC、原始包、算法版本和厂商私有字段。

## 产品边界

项目属于：

- 个人身体测量数据工具；
- AFU-WL 的开源本地客户端；
- 跨厂商身体测量记录的整理层；
- 可审计的算法实验工具。

项目不属于：

- 医疗诊断或治疗工具；
- 所有健康类别的通用健康平台；
- 厂商私有云的破解客户端；
- 自动读取其他 App 沙盒数据库的工具；
- 所有体重秤的通用蓝牙驱动；
- 社交、排行榜、广告或保险评分平台。

## 判断新功能是否符合宗旨

新功能进入路线图前，至少应回答：

1. 它是否帮助用户接入、理解、整理、核对或带走身体测量数据？
2. 它是否保留来源、原始值和计算属性？
3. Apple 健康或系统功能是否已经足够完成？
4. 它是否需要更多敏感权限或网络上传？
5. 它是否可能把估算误导成测量或医疗结论？
6. 它能否避免修改、删除或污染原始数据？
7. 它在没有特定厂商账号或硬件时能否测试？
8. 它的长期维护成本是否与真实使用价值匹配？

如果一个功能主要增加装饰、噱头或指标数量，却不能改善上述问题，就不应成为开发优先项。

## 成功标准

项目不以支持多少品牌、显示多少指标或拥有多少主题衡量成功。成功意味着用户能够清楚回答：

- 我的数据来自哪里？
- 哪些是直接测量，哪些是换算或估算？
- 同一次称重是否被重复保存？
- 换秤、换 App 或换算法是否影响了趋势？
- 哪些数据没有进入 Apple 健康，能否由我确认后补充？
- 我能否导出并迁移自己的完整记录？
- 即使不使用厂商云端，我是否仍能长期掌握自己的数据？

`VISION.md` 定义长期宗旨；[ROADMAP.md](ROADMAP.md) 决定实现顺序；[CHANGELOG.md](CHANGELOG.md) 记录已经发生的变化。路线图可以调整，但不得在没有明确讨论的情况下违背本宗旨。

